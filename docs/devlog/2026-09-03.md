## 21:38 〜 npm タスク検出エラーへの対処

### やったこと
- `package.json` は JSON として正しいことを確認した
- bun 専用なので `.vscode/settings.json` で `npm.autoDetect` を切った
- `version` フィールドを足した

### 詰まった点・気づき
- Cursor の npm 拡張は `package.json` を開けなかったときも「解析できませんでした」と出す。実ファイルの構文エラーではない

## 21:43 〜 README を追加

### やったこと
- 説明と使い方を `README.md` に書いた

### 関連ADR / コミット
- ADR-0001
