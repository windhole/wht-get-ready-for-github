## 02:26 〜 Go 標準ライブラリへの移植

### やったこと
- ADR-0001 を superseded にし、ADR-0002 で Go + stdlib のみに切り替える判断を記録した
- bun / TypeScript を削除し、Go の単一バイナリ CLI に移植した
- プレビューと `--init` の振る舞いは維持し、テストを追加した

### 関連ADR / コミット
- ADR-0002

## 03:29 〜 README を Go 版の手順に合わせて整理

### やったこと
- ビルド → 配置 → 使い方の順に直した
- bun 前提の書き方を残さないようにし、`--help` と `origin` 済みの扱いを追記した

## 07:00 〜 make と GitHub Releases

### やったこと
- `make` で darwin/arm64 の `grg` をビルドするようにした
- `make release VERSION=vX.Y.Z` でタグ・ビルド・gh release まで行うようにした

### 関連ADR / コミット
- ADR-0003

## 07:06 〜 リリース版数の自動加算

### やったこと
- git タグを正として、`make release` はパッチを自動で上げるようにした
- 次の版だけ見る `make next-version` を追加した

### 関連ADR / コミット
- ADR-0004

## 07:16 〜 リリース用 make の名前を分かりやすくした

### やったこと
- `BUMP` をやめ、`make release-minor` / `make release-major` にした
- `make show-version` で現在のタグと次の版番号を出すようにした
